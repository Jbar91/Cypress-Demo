export type MenuOptions = {
  label: string;
  path: string;
};

export type User = {
  userName: string;
  user: string;
  email: string;
  id: string;
};

export type Memes = {
  title: string;
  score: number;
  image: string;
  id: string;
};
